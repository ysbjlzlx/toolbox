import { styled } from '@mui/material/styles';

export const Block = styled('div')({
  position: 'relative',
  padding: '24px',
  margin: 'auto',
  border: '1px solid rgba(0, 0, 0, 0.12)',
  borderRadius: '10px',
});
