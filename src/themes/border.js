import {createTheme} from '@mui/material/styles';

const theme = createTheme({
  border: {
    position: 'relative',
    px: 2,
    mt: 2,
    mb: 2,
    border: '1px solid rgba(0, 0, 0, 0.12)',
    borderRadius: 2,
  },
});

export {theme};
